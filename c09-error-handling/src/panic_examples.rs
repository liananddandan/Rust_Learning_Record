// Demonstrates unrecoverable errors with `panic!` and out-of-bounds indexing.
pub fn run() {
    // Example 1: indexing out of bounds panics at runtime.
    // Commented out so `cargo run` doesn't abort the whole program.
    //
    // let v = vec![1, 2, 3];
    // println!("{}", v[99]); // This would panic.

    // Example 2: explicit panic with a custom message.
    // Also commented out to keep the demo running.
    //
    // panic!("Something went terribly wrong...");

    println!("(panic examples are commented out to avoid aborting the demo)");
}
